---
name: juguang-note-creative-analysis
description: "将聚光投放原始数据整理为按笔记与创意归因的分析工作簿；适用于需要素材拆解、ROI 漏斗诊断和具体投放建议的任务。"
---

# 聚光笔记创意分析

将供应商导出的投放 CSV 或 Excel 转成可复核的“笔记 ID × 创意 ID”分析工作簿。适用于小红书聚光的店铺成交、表单/留资等投放；不用于泛化的品牌策略或没有投放明细的数据。

## 开始前：强制确认供应商与分析日期

用户提供原始文件后、读取数据和生成工作簿前，必须先确认以下两项。若用户已在当前请求中明确给出，可直接引用其答案，不重复提问；否则只发送这两个问题：

1. `这是哪家供应商导出的聚光平台数据？`
2. `本次需要分析的日期范围是？请按 MMDD-MMDD 填写，例如 0827-0909。`

收到答案后，先识别原始数据的日期字段并按该范围**含首尾日期**过滤，再进行任何汇总、排名、分析和导出。原始数据工作表、所有明细表、合计行和创意汇总必须只包含过滤后的记录；不得把范围外日期留在表内，也不得静默改用全量数据。若日期无法解析、跨多个年份而年份无法确定，或范围内没有数据，停止生成并说明可用日期范围。

输出文件名固定为 `【{供应商}】-笔记&创意-数据{MMDD}-{MMDD}.xlsx`，例如 `【供应商A】-笔记&创意-数据0827-0909.xlsx`。表内标题/副标题也应写明同一数据范围。

日期过滤完成后再检查字段、数据粒度、笔记 ID、创意 ID 与转化字段。仅在仍有关键口径缺失时，向用户发送 [供应商问题清单](references/supplier-questionnaire.md) 中与当前文件缺失项相关的问题；不要重复询问文件中已经明确的信息。

在拿到必要答复前，不臆造 ROI、订单、表单或视频内容。若用户无法补充，明确标为“待确认”或留空。

## 分析与工作簿规则

按 [工作簿规范](references/workbook-spec.md) 生成或更新工作簿。关键要求：

- 原始数据保留在独立工作表；除用户明确要求移除的字段外，不改写其原始值。
- 同笔记 ID 视为同一篇内容：先按笔记 ID 汇总素材表现；再单独按创意 ID 汇总，以便比较定向、单元与计划。
- 只保留当前数据或用户需要的转化指标。没有“行业商品 ROI”时绝不补建该列；用户要求去除时，连同公式、条件格式和残留列一起移除。
- 指标使用真实数值和正确的单位/格式。ROI 必须以相同归因口径的 GMV ÷ 消费计算，不能混用归因窗口。
- 截图列默认留空，供用户粘贴。对每个可访问的笔记链接，先自动打开页面核对标题、封面和可见内容/视频，再写视频拆解；不能因为用户没有单独提供截图或正文而直接写“待补”。无法访问或无法加载内容时，写明已尝试的链接核对状态和具体缺失项，不虚构画面。
- 投放分析先给出 `【结论】能卖`、`【结论】不能卖` 或 `【结论】暂不能判定为能卖`。结论后只写对应的 `【为什么能卖】` 或 `【为什么不能卖】`，再用 `【和数据对应】` 和 `【动作】` 给出可执行建议。不能把所有内容都写成“能卖”。
- ROI 颜色默认：高效（≥10）绿色、观察（1–<10）黄色、低效（<1）红色；如用户或供应商给出目标，以其目标为准。合计行不套用红黄绿，保持汇总样式。

## 执行与校验

使用 spreadsheet 工具处理 `.xlsx`，并保持现有模板的工作表、表头、配色与冻结窗格。每次输出前：

1. 核对笔记 ID、创意 ID、日期范围和汇总笔数是否与原始数据一致。
2. 对消费、展现、点击、GMV、订单/表单和 ROI 做抽样复算；总计必须由同一批明细汇总而来。
3. 检查同一笔记的内容链接、截图位置和视频拆解是否对得上。
4. 扫描公式错误，检查删除字段后的右侧残留公式、数值和条件格式。
5. 渲染受影响工作表，确认显示网格线，表头至合计行的每个单元格都有完整细边框，且格式统一、列宽和行高可读、无截断或空白残留。

## 交付

交付一份 `.xlsx`，简要说明数据范围、缺失项和已采用的 ROI 阈值。不要在工作簿、文件名或回复中泄露用户的品牌、供应商、账号、素材链接 token 或本地绝对路径，除非用户明确要求保留。
